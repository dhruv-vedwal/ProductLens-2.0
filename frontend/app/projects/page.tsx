"use client";
import {FormEvent,useEffect,useState} from "react"; import {AppShell} from "../../components/studio/AppShell";import {apiFetch} from "../../services/api";
type Project={id:string;name:string;request_count:number;created_at:string};
export default function Projects(){const [items,setItems]=useState<Project[]>([]),[name,setName]=useState(""),[editing,setEditing]=useState<string|null>(null),[draft,setDraft]=useState(""),[error,setError]=useState("");const refresh=()=>apiFetch("/projects").then(r=>r.ok?r.json():[]).then(setItems);useEffect(()=>{void refresh()},[]);async function create(e:FormEvent){e.preventDefault();setError("");const r=await apiFetch("/projects",{method:"POST",body:JSON.stringify({name})});if(!r.ok){setError((await r.json()).detail??"Could not create project");return}setName("");void refresh()}async function rename(id:string){const r=await apiFetch(`/projects/${id}`,{method:"PATCH",body:JSON.stringify({name:draft})});if(!r.ok){setError((await r.json()).detail??"Could not rename project");return}setEditing(null);void refresh()}return <AppShell><main className="content"><div className="pageIntro"><div><p className="kicker">PROJECTS</p><h1>Give every launch a home.</h1><p>Projects keep the brief, runs, evidence, and final videos separated by initiative.</p></div></div><section className="formLayout"><form className="formPanel" onSubmit={create}><p className="kicker">NEW PROJECT</p><h1>Name the workspace.</h1><label>Project name<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Q3 product launch"/></label>{error&&<div className="formError">{error}</div>}<button className="button">Create project <span>→</span></button></form><aside className="videoPanel"><p className="kicker">YOUR WORKSPACES</p>{items.map(x=>editing===x.id?<form className="projectEdit" onSubmit={e=>{e.preventDefault();void rename(x.id)}} key={x.id}><input autoFocus value={draft} onChange={e=>setDraft(e.target.value)}/><button className="textLink">Save</button><button type="button" className="textLink" onClick={()=>setEditing(null)}>Cancel</button></form>:<div className="runRow" key={x.id}><div><b>{x.name}</b><small>{x.request_count} demo request{x.request_count===1?"":"s"}</small></div><button className="textLink" onClick={()=>{setEditing(x.id);setDraft(x.name)}}>Rename</button></div>)}</aside></section></main></AppShell>}
