[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "productlens-generation-engine"
version = "0.1.0"
description = "ProductLens 2.0 reliability-first generation engine"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.12,<3",
  "structlog>=25.4,<26",
  "httpx>=0.28,<1",
  "playwright>=1.55,<2",
  "fastapi>=0.128,<1",
  "uvicorn[standard]>=0.40,<1",
  "dramatiq[rabbitmq]>=1.17,<2",
  "SQLAlchemy>=2.0,<3",
  "alembic>=1.17,<2",
  "psycopg[binary]>=3.2,<4",
]

[project.optional-dependencies]
dev = ["pytest>=8", "pytest-asyncio>=0.24", "ruff>=0.8", "mypy>=1.11,<2"]
storage = ["boto3>=1.35,<2"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
markers = ["integration: runs a real local browser against supplied fixtures"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
# FastAPI declares dependency injection through these evaluated defaults.
# They are framework contracts rather than mutable application defaults.
ignore = ["B008"]

[tool.mypy]
python_version = "3.11"
strict = true
ignore_missing_imports = true
files = [
  "app/execution/state_diff.py",
  "app/execution/spatial_index.py",
  "app/planning/form_dependencies.py",
  "app/services/stage_contracts.py",
  "app/config/settings.py",
  "app/providers/interfaces.py",
  "app/services/knowledge.py",
  "app/quality/video.py",
  "app/quality/synchronization.py",
  "app/quality/coverage.py",
  "app/quality/story.py",
  "app/quality/consistency.py",
  "app/quality/multimodal.py",
  "app/quality/presentation.py",
  "app/presentation/director.py",
  "app/presentation/scenes.py",
  "app/presentation/journey.py",
  "app/auth/security.py",
  "app/credentials/service.py",
  "app/browser/screencast.py",
  "app/video/source_timing.py",
  "app/contracts/models.py",
  "app/workers/broker.py",
  "app/workers/local.py",
  "app/workers/tasks.py",
  "app/services/jobs.py",
  "app/services/render_stage.py",
  "app/persistence/repository.py",
]

[tool.hatch.build.targets.wheel]
packages = ["app"]
